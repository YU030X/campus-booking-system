## 1. Dependency and ownership gates

- [x] 1.1 Gate integration on the sibling planning artifacts: T04 `../add-web-auth-shell/`, T05 `../add-web-resource-management/`, T06 `../add-resource-availability/`, and T07 `../add-concurrent-booking-core/` all currently exist but are unmerged planning changes. Merge or rebase each into the implementation worktree, then reread its authoritative route, DTO, payload, status, and error contracts; record drift and stop before writing booking integration code. Evidence: branch fast-forwarded to `main` for merged T04/T05/T06 and merged T07/T10/T09 dependency chain in `c880c6d`; authoritative controllers/DTOs/views/messages were reread with no contract drift.
- [x] 1.2 Obtain the T04 shared-file handoff that connects `/bookings` and `/bookings/:id` route components to the T08 views; do not edit `router/index.js`, `api/http.js`, auth shell, resource views, shared contracts/types, package manifests, or backend. Evidence: a separately delegated T04 owner changed only `booking-web/src/router/index.js`, replacing the two existing Placeholder components while preserving paths, roles and guards; production build passed.
- [x] 1.3 Keep all T08 implementation and tests under the owned booking view/component and booking-domain API/store paths; stop and report if a required change crosses that fence. Evidence: T08 files are limited to booking API/store, booking components/views and `tests/booking/**`; the sole shared router edit is the separately recorded T04 handoff.

## 2. Booking domain transport and pure models

- [x] 2.1 Define the booking-domain API boundary for availability, create, list, and detail using the frozen paths and canonical envelope; route every call through the shared HTTP client, preserve Long IDs as strings, and keep T07's backend error code/message pair intact for UI mapping. Evidence: `src/api/booking.js` exposes fixture-injectable and real shared-client adapters for availability/create/list/detail/cancel with canonical envelope checks and string-ID validation.
- [x] 2.2 Add pure mappers/validators for the T06 availability payload and T07 create/list/detail DTOs, including `yyyy-MM-dd`/`yyyy-MM-dd HH:mm:ss`, exact status enums, pagination limits, and `resourceId` string handling. Evidence: `components/booking/validation.js`, `slots.js`, and `errors.js` enforce exact field sets, calendar/time formats, seven statuses, 1..100 pages, 30-minute intervals and combined 409/code/message mapping.
- [x] 2.3 Add fixture-backed unit tests for mapper normalization, unknown fields, malformed identifiers, and canonical error mapping without extending the T04 global mock or fabricating booking success. Evidence: local Node fixtures cover these cases; `authMock.js` is unchanged and booking failures are never converted to success.

## 3. Slot selection and creation workflow

- [x] 3.1 Implement the booking creation state model for same-day available slots only; enforce `slotMinutes=30`, `:00`/`:30` alignment, disabled/past-slot rejection, and contiguous selection. Evidence: `slots.js`, `SlotPicker.vue`, and `CreateBookingPanel.vue` reject malformed/unavailable/past/non-contiguous choices and permit no arbitrary time entry.
- [x] 3.2 Derive start/end from the first and last selected slot with left-closed/right-open semantics, display the duration, trim purpose and convert blank to null, enforce non-null purpose at most 500 Unicode code points and attendeeCount>=1, and serialize the create request exactly. Evidence: pure selection/request validation and the create panel implement all boundaries; astral Unicode 500/501 tests pass.
- [x] 3.3 Add loading/deduplication state so repeated submit activation cannot issue a second request; cover valid, invalid, boundary, and duplicate-submit cases with pure store/component tests. Evidence: `stores/booking.js` deduplicates the create key and disables pending UI; synchronized fixture test proves two activations issue one create call.
- [x] 3.4 Handle HTTP 201 by refreshing the current-user list and affected availability. For HTTP 409/code 43000, require T07's backend message too: `该时段已被占用，请刷新后重试` maps to `该时段刚被其他人预约，请刷新` and refreshes slots; `当前预约请求较多，请稍后重试` maps to system-busy feedback without calling it a slot conflict. Never branch on code alone or treat availability as final correctness. Evidence: store success refreshes list/date slots; error classifier requires status+code+exact message and refreshes only the slot-conflict branch.

## 4. My bookings list and detail

- [x] 4.1 Implement `/bookings` list state for current-user records with explicit pageNumber/pageSize<=100 and exact status filtering, plus loading, empty, error, retry, and stable pagination behavior. Evidence: `views/my-bookings/List.vue` and booking store implement explicit filters/page state and all UI states.
- [x] 4.2 Implement `/bookings/:id` detail loading and exact `BookingView` rendering with the seven shared statuses and a non-invented status timeline; reject unsafe/malformed IDs before transport and map 404 to not found. Evidence: `Detail.vue` renders all 14 fields; `StatusTimeline.vue` displays only the returned current status/timestamp; unsafe IDs make no API call and 404 maps to not found.
- [x] 4.3 Add list/detail tests for pagination, status filters, Long-string IDs, empty/error/retry states, not-found behavior, and prevention of arbitrary status transitions. Evidence: mapper/API/store fixtures cover page/status/empty/errors/string IDs/unsafe detail; UI exposes no target-status operation.

## 5. Safe query handoff and cancellation gate

- [x] 5.1 Support same-origin `/bookings?resourceId=<safe-id>&date=<yyyy-MM-dd>` creation handoff only after validating both values; ignore unsafe schemes, malformed dates, unknown data, and untrusted origins without fabricated resource or slot state. Evidence: `parseBookingHandoff` accepts only the exact two valid scalar keys; safe/unsafe query fixtures pass.
- [x] 5.2 Add the T09 cancellation capability gate with disabled/unavailable UI and zero transport calls before the authoritative contract is merged and reread. Evidence: no cancellation code existed before the T09 merge; implementation was enabled only after rereading the merged authoritative controller/DTO contract.
- [x] 5.3 After T09 is merged, enable cancellation only through the booking API module; cover success refresh of list/detail/availability and 401/403/404/409 mappings, preserving the session for 403. Evidence: cancel UI permits only active pre-start states; API validates ≤200-code-point reason; store refreshes list/detail/availability; error tests prove 401/403/404/409 classification and 403 `sessionAction:null`.

## 6. Feature-gated acceptance evidence

- [x] 6.1 Before backend gates are available, run only pure store/API mapper/component tests with local fixtures and record that no real integration or global mock expansion was performed. Evidence: Node fixture suite uses injected API transport/provider only; no global mock or package manifest changed.
- [ ] 6.2 After T06/T07/T09 feature gates are available, run headless CLI browser QA for contiguous-slot boundaries, disabled/past slots, loading deduplication, 201/409, pagination/status/timeline, safe/unsafe query handoff, cancellation gate, and post-action refresh; capture screenshots and request/response evidence without opening windows or popups. Current gate: production routes and build are ready, but the bounded real browser run requires temporarily starting backend/frontend long-running processes; no mock evidence is being substituted.
- [x] 6.3 Run the frontend build from `booking-web`, `openspec validate add-web-student-booking --type change --strict`, and `git diff --check`; record exact results and stop on any contract drift or validation failure. Evidence: final Node fixture suite 55 tests/18 suites passed with zero failures/skips; `npm run build` succeeded (only existing chunk/dynamic-import warnings); strict OpenSpec validation reports valid and `git diff --check` is clean.
- [x] 6.4 Review the final diff and status to confirm only T08-owned implementation/tests, this change's planning artifacts, and the separately owned T04 route handoff are present; do not commit, push, or archive until the user chooses the Git action. Evidence: independent OpenCode review found no P0; its two P1 findings (post-success refresh misreporting and stale detail races) were fixed and independently rechecked with 7/7 store regressions green. Scope review confirms only owned T08 paths plus the single-file T04 router handoff; pre-existing staged `docs/**` remains untouched.
