## 1. Preconditions and ownership gates

- [x] 1.1 Record the exact baseline commit (`0e53b7efec27f5821056bd546b8a245144414fdb`) and confirm the dedicated worktree/branch; do not reinterpret the missing `main0e53b7e` ref as a different commit.
  - Evidence (slice 5): baseline + absolute worktree path + branch `codex/verify-and-deploy-system` recorded in `deploy/evidence/preconditions.md` §1; missing ref name documented as a missing ref, not a different commit.
- [x] 1.2 Index the existing T04–T12 and Redis-sibling planning artifacts by owner/path, then collect proof of P0 merge, rebase onto the selected baseline, and spec sync; record T12 optional cuts (statistics → notifications → cache). Existing but unmerged or unsynced planning artifacts do not satisfy the evidence gate; keep the change Draft when any proof is missing.
  - Evidence (2026-08-31): integration merge `19649b5` contains the accepted T04-T12 chain; `070155f` synchronizes all discovered delta capabilities into main specs; strict main-spec validation passed 21/21. T12 cut matrix passed all three cuts with booking/T07 82/82 at each stage. The change remains Draft for later runtime/external gates, not for missing integration/spec-sync proof.
- [x] 1.3 Publish an ownership map for allowed T13 paths (integration/E2E tests, JMeter, `deploy/**`, deployment/performance/runbook/demo docs) and a stop-and-request template for T01–T12 defects or shared-file changes.
  - Evidence (slice 5): ownership map (allowed/forbidden paths) + stop-and-request template published in `deploy/evidence/preconditions.md` §4–5; live requests tracked in `deploy/owner-change-requests.md`.
- [x] 1.4 Select and document the approved headless CLI/browser runner available in the apply environment; if unavailable, mark E2E execution blocked rather than substituting unapproved tooling silently.
  - Evidence (authorized local run, 2026-08-28): `pwsh scripts/tests/t08/run.ps1 -Action Check` exited `0` with `CHECK_OK`; runner is the existing T08 harness using Chrome `--headless=new`/raw CDP. No browser flow was claimed or executed by this check.

## 2. Integration and headless E2E evidence

- [x] 2.1 Create an isolated test profile that injects generated credentials, points at disposable MySQL/Redis services, and defaults to non-public endpoints.
  - Evidence (2026-08-31): the committed template keeps ENV names only; an ignored runtime instance supplied loopback `127.0.0.1` endpoints and generated/local test credentials to real MySQL 8.0.46 and Redis 7.0.15. API and browser runs consumed it successfully; no public endpoint was permitted.
- [x] 2.2 Implement API integration coverage for register/login, password non-disclosure, resource/category/slot browsing, direct and pending booking creation, approval/rejection, cancellation, check-in, no-show/violation, terminal slot release for `REJECTED`/`CANCELLED`/`NO_SHOW`, refresh/persistence, and idempotency; cover `COMPLETED` release only when an approved optional automatic-completion feature is enabled, never as a P1 prerequisite.
  - Evidence (2026-08-31): the fixed 37-class inventory executed through `deploy/e2e/run.ps1`; 195/195 passed with zero failure/error/skip. Ignored raw evidence: `deploy/artifacts/e2e-ApiIntegration-t13-api-integration-pass/`.
- [x] 2.3 Implement boundary coverage for missing/malformed/expired credentials, student-to-admin 403, cross-owner read/cancel, invalid transitions, missing resources, and same-slot 409 conflicts; assert canonical envelopes and no unauthorized mutation, classifying HTTP 409/code `43000` `SYSTEM_BUSY` by message/category as a separate error.
  - Evidence (2026-08-31): the API inventory's auth/ownership/transition/concurrency classes passed within 195/195; StudentBrowser additionally passed real 409 slot-conflict, 409 SYSTEM_BUSY, recovery 201, unsafe-id zero-transfer, 401 redirect, and 403 session-preservation cases.
- [x] 2.4 Implement headless browser flows for both direct and approval paths, including route refresh after each state change; capture screenshots and network traces with Authorization, cookies, passwords, and PII redacted.
  - Partial (2026-09-02): direct StudentBrowser passed 15/15; text redaction residual is zero and all 52/52 PNGs were manually reviewed as generated QA-only data. Evidence: `deploy/artifacts/e2e-StudentBrowser-t13-student-browser-final/`. A read-only T11 audit found a real approve/reject raw-CDP candidate but confirmed fixed credentials, no finally teardown, incomplete Authorization/cookie/PII redaction, and no approve/reject route-refresh+API-reload proof. T13 now has a 32-assertion offline Approval intake contract (independently security- and runtime-reviewed) requiring a repository-local reparse-free owner root, a repository-local .exe/.ps1 executable, positional RunId/output root, fresh manifest, six exact refresh cases, strict-boolean cleanup, safe/distinct screenshot/network paths, fail-closed redaction (including unscannable binary files) and permanent `EXECUTED_UNPROVEN`. Real approval-path execution (2026-09-04, user decision 4 closed OCR-8 owner attestation): T11 owner runner `scripts/tests/t11/approval-contract-runner.ps1` (runtime-generated credentials, scoped cleanup-first fixture, atomic arm/confirm against the polling watcher) was merged (`349e070`+`bff8464`) and executed through the hardened intake lane `deploy/e2e/run.ps1 -Mode ApprovalBrowser`: commandExit=0, redactionExit=0, contractComplete=true, errors=[], status=EXECUTED_UNPROVEN/exit 2 (by design), evidence `deploy/artifacts/e2e-ApprovalBrowser-approval-real-20260904/` with six refresh cases (admin login, pending list, approve, reject, approved/rejected student detail) each proving route refresh + API reload persistence, 6/6 PNGs manually reviewed (`REVIEW-RECORD.txt`), exact-scope DB cleanup verified. Direct path: StudentBrowser 15/15. Both paths now hold real browser evidence; combined task is complete.
- [x] 2.5 Link each E2E artifact to a requirement and record failures as owner change requests without editing business/frontend/common source in T13.
  - Evidence (2026-08-31): `deploy/e2e/inventory.md` maps requirements to API classes/T08 cases; `deploy/evidence/verification-matrix.md` links the two executed artifact bundles. T10/T08 evidence defects were fixed in their owner branches and recorded as OCR-10/OCR-11; ApprovalBrowser remains OCR-8.

## 3. JMeter concurrency experiment

- [x] 3.1 Add parameterized JMeter plans for the isolated vulnerable baseline, unique-index-only round, and unique-index-plus-Redisson round using clean data scopes.
  - Evidence (2026-09-01, implementation contract): `booking-concurrency.jmx`, `rounds.example.json`, and `run.ps1` define the three ordered history mirrors, property-driven mutually exclusive scenarios, runtime-only fixtures, and pre/post scope counts. `contract-tests.ps1` parses the JMX/template and verifies the required baseline/index-only/Redisson declarations. This completes the authored-plan deliverable only; no real round is claimed.
- [x] 3.2 Configure the same-slot case for 100 concurrent requests and a distinct-resource/date case for lock-granularity comparison; ensure the baseline cannot reach public or production endpoints.
  - Evidence (2026-09-01, implementation contract): the offline suite verifies both ThreadGroups are exactly 100 threads/ramp 1/loop 1, both POST the canonical booking path, non-loopback URLs are refused, baseline enablement/isolation/operator-consent gates all fail closed, 99-row distinct CSV is refused, and a valid 100-row distinct CSV passes fixture validation. Latest focused run passed 44 assertions without JMeter, Docker, or HTTP.
- [x] 3.3 Add result extraction for success, HTTP 409 business conflicts, HTTP 409/code `43000` `SYSTEM_BUSY` errors (classified by message/category), 500/connection/data errors, average/P95/P99 latency, thread/ramp settings, environment versions, and final booking/slot row counts.
  - Evidence (2026-09-01, implementation contract): `summarize.ps1` now fails closed on missing/invalid execution, environment, pre/post database, or raw-log metadata; emits mutually exclusive classifications, average/P95/P99, thread/ramp/loop, versions, Redis/seed/history state, pre/post/delta counts, and explicit JTL/log/metadata links without raw response bodies. A non-zero JMeter exit retains a diagnostic report but can never pass. Synthetic protected, SYSTEM_BUSY, baseline, distinct, boundary, bad-delta, non-zero-exit, missing-metadata, and missing-log cases all pass the 45-assertion contract suite. No real JTL/performance result is claimed.
- [ ] 3.4 Produce a three-round report where exactly one success and 99 business conflicts is asserted only for each healthy-Redis, valid-seed, same-slot protected round; label baseline duplicates as historical vulnerability evidence, and count `SYSTEM_BUSY` separately as an error.
  - Partial (2026-09-01): the 45-assertion offline suite proves the exact protected 1/99/0, SYSTEM_BUSY separation, baseline historical label, distinct report-only scope, row-delta, non-zero-exit, and privacy/fail-closed logic against synthetic JTL. It is not performance evidence. JMeter 5.6.3 plus the fixture/history artifacts (OCR-5/6/7) remain absent, so no real three-round report exists and this task stays unchecked.

## 4. Container and Nginx deployment plan

- [x] 4.1 Add the JDK 17 backend Dockerfile and frontend build-plus-Nginx Dockerfile with pinned non-floating base references, digest recording, non-root settings where practical, and no secret material.
  - Evidence (2026-09-03): `docker compose ... build --pull=false api edge` rebuilt both images from the current checkout with immutable linux/amd64 runtime tag@RepoDigest references and exited 0. `deploy/artifacts/t13-image-build-20260903-runtime-refresh/` records the build log, Docker 29.4.3, verified runtime RepoDigests, and non-root application users (`10001:10001` API, `101` edge). Earlier failed pull logs are historical refresh evidence only.
- [x] 4.2 Add Compose services for Nginx/API/MySQL 8/Redis with private DB/Redis networks, no MySQL/Redis host ports, public 80/443 only, named volumes, healthchecks, restart policies, dependency ordering, resource limits, and bounded logging.
  - Evidence (2026-09-01): config validation passes; `deploy/artifacts/t13-runtime-current-20260901/` proves all four services healthy, only edge published at loopback `127.0.0.1:18080`, API expose-only, MySQL/Redis without host ports, and the backend network internal. `t13-restart-persistence-20260901-audited` additionally proves health-gated restart with volumes preserved.
- [x] 4.3 Add Nginx SPA fallback and `/api` proxy configuration with security headers, request-body limits, upstream timeouts, internal-address hiding, and certificate/key mounts without committed keys.
  - Evidence (2026-09-01): `t13-runtime-current-20260901` proves the default local edge returns 200 for health/deep SPA, proxies unauthenticated `/api` to 401, emits security headers, rejects 3 MiB with 413, and returns 504 for a paused upstream at the configured timeout. `compose.tls.yml` + `nginx/tls.conf` add an explicit, inert-by-default 443→8443 overlay using operator file-backed Compose secrets at `/run/secrets/tls.crt` and `tls.key`; no key is committed. The TLS overlay checker asserts topology, secret targets, read-only config, Nginx references, and fail-closed missing cert/key paths while explicitly recording no container, certificate, HTTPS, or public endpoint execution. Real TLS/public acceptance remains tasks 8.1-8.2, not this implementation claim.
- [x] 4.4 Add a non-secret `.env.example` documenting required runtime variables, image pin/digest update procedure, certificate renewal steps, and the distinction between local compose validation and external deployment acceptance.
  - Evidence: `deploy/.env.example`, `deploy/.gitignore`, and `deploy/README.md` provide placeholders only, the digest/renewal procedures, and explicit local-vs-external gates. Compose config consumed ignored runtime values successfully; no secret value was committed.

## 5. Migration, backup, restore, and recovery runbooks

- [x] 5.1 Add an empty-database verification script/runbook that applies V001–V005 to two fresh MySQL 8 databases, asserts exactly twelve tables/required indexes/no seed, and proves identical definitions without editing migrations.
  - Evidence (2026-09-01): audited run `t13-empty-migration-20260901-final` used exact image digest `mysql@sha256:d58ac...`, `--pull never`, and two isolated `--network none` lanes. Both applied V001–V005 and produced the exact 12-table InnoDB/utf8mb4 schema, all 34 declared keys, zero rows, and identical SHA256 fingerprints. `result.json` records HEAD, cwd, PowerShell/Docker versions, image digest, timestamps and exit 0; no migration was edited.
- [x] 5.2 Add a consistent MySQL backup/restore runbook with isolated restore target, schema/booking/slot checks, checksum or row evidence, and explicit operator-recorded RPO/RTO assumptions.
  - Evidence (2026-09-01): audited run `t13-backup-restore-20260901-nonzero` records the exact source image digest, HEAD/cwd/tool versions/timestamps/exit 0, explicit RPO 24 hours and RTO 4 hours (14400 seconds), and measured restore 2.131 seconds with `restoreWithinRto=true`. The isolated restore matched all 12 definitions/checksums plus a real fixture-owned booking (1→1) and its two 30-minute slot rows (2→2) with identical aggregates; cleanup removed only the fixture scope. Empty/placeholder assumptions fail closed.
- [x] 5.3 Add a volume restart/recreate procedure proving MySQL persistence and health-gated recovery for API/MySQL/Redis; capture logs and compose config output.
  - Evidence (2026-09-01): `t13-restart-persistence-20260901-audited` executed the volume-preserving restart lane. API/MySQL/Redis all returned healthy, MySQL schema fingerprint remained identical, row-count diffs were empty, and compose config/log/pre/post evidence was captured. The script emitted no `down`, `-v`, container removal, or volume removal command.
- [x] 5.4 Add Redis-failure behavior checks for both consumers: T07 booking-lock outage MUST fail closed with HTTP 409/code `43000` `SYSTEM_BUSY` and MUST NOT use DB-only fallback; T12 availability/cache outage MAY fall back to MySQL. Prove database uniqueness and authorization remain correct and record message/category, latency, and recovery evidence.
  - Evidence (2026-09-01): final run `t13-redis-outage-20260901-final` used a generated in-memory student credential plus the owner-scoped T08 resource fixture against loopback Docker runtime. With Redis stopped, T07 returned HTTP 409/code 43000/exact SYSTEM_BUSY, booking/slot/window counts remained unchanged, and T12 availability returned HTTP 200/code 0/data from MySQL fallback with zero mutation. Redis restarted and returned PONG. `result.json` records HEAD/cwd/tool versions/timestamps/exit 0; earlier runs exposed and fixed two T13-only harness bugs (PowerShell backtick SQL corruption and object-output contamination).
- [x] 5.5 Add conservative image/config rollback and recovery steps that preserve volumes and treat committed migrations as forward-only unless an owner-approved recovery plan exists.
  - Evidence: `deploy/runbooks/recovery.md` §5 specifies digest-pinned image/config rollback, volume preservation, no `down -v`, forward-only migrations, and mandatory health/smoke rechecks. This checks the documentation deliverable only; a real drill remains blocked with Docker runtime gates.

## 6. Demo orchestration and evidence

- [x] 6.1 Request or reference a T01-owned seed change or define an ephemeral runtime fixture; keep the empty-migration lane seed-free and stop if ownership is not approved.
  - Evidence (2026-09-04): the user explicitly authorized the ephemeral demo fixture (session decision 2, recorded 2026-09-03). The fixture stays runtime-created and seed/migration-independent: the empty-migration lane was never seeded, and the T08 `seed.sql` is consumed only as an owner reference by the T08 harness itself inside its own `t08qa_*` namespace. Demo namespace `t13demo_*` ran with zero collisions across runs g/j (preflight held), so the ownership gate is closed.
- [x] 6.2 Generate deterministic non-PII admin/student/resource/booking/violation data with strong runtime-injected passwords; redact credentials from logs and evidence.
  - Evidence (2026-09-04, real run t13demo-20260903-j): Setup generated 3 fixed-role users (32-byte random passwords, never printed/persisted), 128-bit ownership-tag-bound resource, deterministic PENDING booking (2 slots), and the past CONFIRMED booking whose NO_SHOW violation was produced by the OWNER scan (owner-owned, demo teardown verified its removal). Passwords lived only in the same-run system-random temp JSON; `fixture-map.json`/journal contain ids/usernames/purposes only; command/e2e logs carry no credential values. Residual no-show scan behavior: the periodic owner scan may legitimately release the past booking's slots; Setup/Teardown tolerate 0..2 surviving past slots while the pending slots stay exactly 2 (deterministic).
- [x] 6.3 Add setup/teardown orchestration scoped to the fixture-owned database/volume and a demo script covering the required lifecycle and authorization steps.
  - Evidence (2026-09-04, real execution): full `All` lifecycle exit 0 — Setup populated the exact fixture, StudentFlow ran the real browser lane, Teardown removed every fixture-owned row with `T13TD:0:1:1` and leftover=0 across users/bookings/children/resource/category (evidence `deploy/artifacts/demo-All-t13demo-20260903-j/`). Standalone interrupted-run teardown (run-h, killed mid-StudentFlow) also compensated the exact scope with exit 0. The SERIALIZABLE exact-ID-set teardown held against real populated data, including the no-show-slot-release case (0..2 surviving past slots tolerated; deletion still exact-ID). Known residual: a hard kill between mutation commit and journal write can still strand an unjournaled row; during this acceptance one orphan violation from an earlier manual cleanup was found and removed — recorded honestly as operator recovery, not harness behavior.
- [x] 6.4 Produce a redacted evidence bundle mapping screenshots/network traces to acceptance requirements and mark the demo gate Draft when setup, owner approval, or evidence is incomplete.
  - Evidence (2026-09-04): `evidence-index.md` maps the real StudentFlow run (T08 15/15 PASS, `deploy/artifacts/e2e-StudentBrowser-run-20260904-001040/`): REPORT.md (redacted text), network.jsonl (scrubbed), and 52/52 screenshots manually reviewed via 6 contact sheets (`deploy/artifacts/t13-png-review-sheets/REVIEW-RECORD.txt`) — generated `t08qa_*` QA data only, password inputs masked, no real PII/credentials/tokens. OCR-8 Approval rows remain blocked/NOT RUN inside the bundle, keeping the approval gate Draft as required; the demo lifecycle gate itself is complete. ApprovalBrowser owner lane remains under OCR-8 and is NOT claimed by this task.

## 7. Local verification gates

- [x] 7.1 Run `cd booking-api && mvn verify` on JDK 17 and preserve the actual output.
  - Evidence (2026-08-31): fresh T13 backend gate on JDK 17 with real loopback MySQL/Redis passed 387/387, zero failure/error/skip. Ignored raw evidence: `deploy/artifacts/verify-backend-t13-backend-pass/`. The earlier OCR-9 compile failure and OCR-10 flaky attempt remain historical evidence only.
- [x] 7.2 Run a clean frontend install/build using the repository-pinned lockfile and preserve the actual output; do not modify package manifests in T13.
  - Evidence (authorized local offline run, 2026-08-28): `cd booking-web && npm ci --offline --ignore-scripts` exited `0`; `npm run build` exited `0`; logs `deploy/artifacts/local-frontend-offline/npm-ci-offline.log` and `npm-build.log`. Vite emitted non-fatal chunk/comment warnings; no package manifest was modified.
- [x] 7.3 Run strict OpenSpec change validation, main-spec validation after any future sync, and `git diff --check`; record unexecuted checks explicitly.
  - Evidence (2026-08-31): change strict validation passed; main-spec strict validation passed 21/21 after sync; `git diff --check` and the reusable static gate passed.
- [ ] 7.4 Run compose config, image/dependency/artifact scan, secret scan, health smoke, empty migration, backup/restore, restart persistence, Redis failure, and local headless E2E checks; attach raw evidence and classify failures.
  - Partial (2026-09-03): Compose config, immutable runtime refresh/build, local SBOM inventories, four-service health/HTTP smoke, TLS-overlay config, backend/frontend, API integration 195/195, StudentBrowser 15/15, empty migration, backup/restore, restart persistence, Redis outage, static/JMeter/Demo/image-scan-validator implementation contracts, and secret/ownership gates have recorded evidence. The real offline Trivy bundle `deploy/artifacts/t13-real-scan-20260903-runtime-refresh/` has fresh global DB metadata and structurally valid reports: runtime base pre-scans are 0/0, edge is 0/0, but API has 7 CRITICAL + 30 HIGH Java application-dependency findings. `deploy/scan/run.ps1 -Action Validate` returns `VALIDATED_SCAN_BLOCKS_RELEASE` (exit 2); application dependency remediation remains owner-scoped and task 7.4 stays unchecked. The current validator does not carry per-finding Java DB provenance, which is retained as residual risk. Demo, JMeter, ApprovalBrowser, and external lanes remain blocked or unexecuted under their existing owner gates.
- [x] 7.5 Review changed paths against ownership and verify no `.env`, key, token, password, PII, build output, migration edit, or public DB/Redis port is present.
  - Evidence (2026-08-31): final tracked-path and secret-value scans found no committed runtime `.env`, key/token/password/PII, generated target/dist/node_modules output, or `sql/` migration edit. `deploy/compose.yml` publishes only the loopback edge and gives MySQL/Redis no host ports; the pre-existing root development Compose binds DB/Redis to `127.0.0.1`, not public interfaces. Owner-chain `pom.xml` history is outside T13-owned edits. Static gate and `git diff --check` passed.

## 8. External acceptance gate

- [ ] 8.1 Obtain explicit user authorization for the target host/domain, DNS ownership, TLS certificate/renewal mechanism, and any required credentials before attempting public acceptance.
  - Partial (slice 6, static only): external acceptance prerequisites and refusal rule are documented; no target, DNS ownership, TLS path, or deployment credentials have been authorized.
- [ ] 8.2 If authorization and infrastructure are provided, run the documented deployment, HTTPS, public smoke, rollback, and monitoring checks without automatic spend/provisioning; otherwise mark these tasks not run/blocked.
  - Partial (slice 6, static only): public deployment is explicitly not run and no automatic provisioning/spend is permitted; external gate remains blocked pending task 8.1 inputs.
- [ ] 8.3 Keep the PR/change Draft until T04–T12 gates, all required local evidence, spec sync, and any authorized external checks are complete; never claim public URL, domain, or certificate completion from local evidence alone.
  - Partial (slice 6, static only): current change remains Draft because merge/spec-sync/owner evidence and all runtime/external checks are incomplete; no public completion claim is made.
