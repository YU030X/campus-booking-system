## 1. Preconditions and ownership gates

- [x] 1.1 Record that the T07 `add-concurrent-booking-core` sibling planning is currently unmerged; before T09 final merge, confirm its merged/rebased contract and record the exact public booking action/release ports and transaction behavior. **Stop and file a T07 owner handoff request if the contract cannot be confirmed; do not edit `booking/**`.** Evidence: apply base is rebased onto merged T07 — commits `0d31cd5 feat: add concurrent booking core`, `cc3dd17 feat: expose approval booking handoffs`; confirmed ports: `BookingActions` (approve/reject/cancel/checkIn/markNoShow, REQUIRED propagation, cancel takes caller actionTime and SQL guards `start_time > #{actionTime}`) at `booking-api/.../booking/service/BookingActions.java:14-24`, outcome model `BookingActionOutcome{WINNER,ALREADY_COMPLETED,ILLEGAL_TRANSITION,NOT_FOUND}+BookingView` at `BookingActionOutcome.java:5-22`, read port `BookingAdminReads.pagePendingApprovals(int,int)` implemented by `DefaultBookingAdminReads` (`deleted=0 AND status='PENDING_APPROVAL' ORDER BY created_at ASC,id ASC`, bounds 1..100), slot release inside winner reject/cancel via `DefaultBookingActions.java:51-72` + `BookingSlotReleaseService.releaseTerminalSlots`. No booking/** edits made.
- [x] 1.2 Record that the T10 `add-checkin-no-show-violation` sibling planning is currently unmerged. T09/T10 may develop in parallel: T10 first merges the transaction-participating `ViolationPort` without waiting for T09; T09 may apply approval, normal cancellation, and port adaptation, then rebase onto T10 and run late-cancel integration before final merge. Evidence: T10 is now merged into the apply base — commit `9a096c7 feat: add check-in and violation lifecycle`; REQUIRED `ViolationPort` at `booking-api/.../violation/port/ViolationPort.java:12-19` with frozen `-10/-5`, caller-transaction participation and `(booking_id,violation_type)` idempotency; consumer precedent `task/NoShowItemProcessor.java:28-35`.
- [x] 1.3 Obtain exact DTO/error handoff notes for T08 and T11 (field names, normalization, 40000/40100/40300/40400/43000 behavior) and preserve them in implementation review notes without changing their owned paths. Evidence: published in section 5.1 below; no frontend paths touched.

## 2. Approval package contract (approval/** only)

- [x] 2.1 Add approval request DTOs with unknown-field rejection, trim/blank normalization, nullable approve/cancel comments, required reject comment, and limits measured in Unicode code points: approve/reject comments 500, cancel reason 200. Evidence: `approval/dto/ApproveRequest.java` (nullable, blank→null, ≤500 code points incl. astral), `RejectRequest.java` (required 1..500), `CancelRequest.java` (blank→null, ≤200); each rejects unknown JSON via `@JsonAnySetter` (same pattern as T07 `CreateBookingRequest`), surfacing as HTTP 400/code 40000 through `GlobalExceptionHandler`. Covered green by `ApprovalRequestTest` (8 tests).
- [x] 2.2 Add BookingView/ApprovalView mappings and controller responses using shared Result/PageResult envelopes, Asia/Shanghai time formatting, and JSON-string Long identifiers. Evidence: `approval/vo/ApprovalView.java` (String ids, Shanghai LocalDateTime serialized by shared `JacksonConfig`); responses reuse T07 `BookingView`; verified in `ApprovalControllerMockMvcTest.pendingListReturnsCanonicalPageWithStringIdsInServerOrder` and approve/cancel success assertions.
- [x] 2.3 Add the four controllers/routes with authentication and ADMIN guards; keep controllers limited to binding, validation, authorization entry, and service calls. Evidence: `ApprovalAdminController` (`GET /api/v1/admin/approvals`, `POST /api/v1/admin/bookings/{id}/approve|reject`) and `BookingCancelController` (`POST /api/v1/bookings/{id}/cancel`); ADMIN enforcement comes from the existing `/api/v1/admin/** hasRole("ADMIN")` chain rule (`auth/security/AuthSecurityConfig.java:48`), 401 entry point and 403 denied handler are shared beans; controllers only parse path ids, bind DTOs, read principal, delegate.
- [x] 2.4 Add the pending-approval query restricted to non-deleted `PENDING_APPROVAL`, page bounds 1..100, and deterministic createdAt/id ascending ordering. Evidence: consumed via `BookingAdminReads.pagePendingApprovals` (SQL at `booking/mapper/BookingMapper.java:50-59`); controller adds `@Min/@Max` guards; bounds rejection asserted in `ApprovalControllerMockMvcTest.pendingPageSizeOutsideBoundsIsRejectedWith40000`.

## 3. Domain action orchestration and transaction boundary

- [x] 3.1 Implement approval service calls to the reviewed T07 approve/reject/cancel action and slot-release ports; do not access booking or booking_slot persistence directly. Evidence: `ApprovalService` injects only `BookingAdminReads`, `BookingActions`, `ViolationPort`, `ApprovalRecordMapper` (approval-owned table); no booking/slot mapper imports anywhere under approval/**.
- [x] 3.2 Ensure approval actions conditionally transition and atomically insert one immutable APPROVE/REJECT record as appropriate; only the winning transition creates the record, and approve retains slots while reject releases them. Evidence: `ApprovalService.conclude` inserts via `ApprovalRecordMapper.insert` only on `WINNER`; slot retention/release is performed inside `DefaultBookingActions.approve/reject` (T07). Unit proof: `ApprovalServiceTest.approveWinnerInsertsOneApproveRecordAndReturnsCurrentView`, `repeatedIdenticalApproveAfterTargetStateInsertsNoSecondRecord`.
- [x] 3.3 Ensure cancel accepts only owner + PENDING_APPROVAL/CONFIRMED before start, physically releases slots in the same transaction, and returns the current view for an already-cancelled duplicate. Evidence: single conditional call `bookingActions.cancel(bookingId, userId, actionTime, reason)`; ownership/start predicates live in T07's `cancelActiveByOwner` SQL (`AND user_id=#{userId} ... AND start_time > #{actionTime}`, `BookingMapper.java:61-68`); `NOT_FOUND` outcome → uniform 40400; `ALREADY_COMPLETED` → current view, no side effects.
- [x] 3.4 Call the T10-required transaction-participating `ViolationPort` only for `< 2h`, with booking/user/start-time context and an idempotency key, in the same transaction as cancel state and slot deletion; implement no violation/user persistence or credit mutation in T09. The T10 port contract applies `LATE_CANCEL = -5`, `NO_SHOW = -10`, and credit floor 0. Evidence: `ApprovalService.cancel` calls `violationPort.recordLateCancel(bookingId, userId)` exactly once on WINNER when `isLateCancel(startTime, actionTime)` (strictly after `startTime.minusHours(2)`; exact 2h exempt — `ApprovalService.isLateCancel`); idempotency key is the frozen `(booking_id,violation_type)` uniqueness inside T10's port; no violation/user writes in approval/**.
- [x] 3.5 Map missing/foreign/deleted cancellation uniformly to 40400, students to 40300 on admin routes, unauthenticated requests to 40100, and illegal/opposite terminal actions to HTTP 409/code 43000. Repeated identical approve/reject/cancel actions return HTTP 200 with the current BookingView and have no side effects. Evidence: outcome switch in `ApprovalService`; `ApprovalControllerMockMvcTest` covers 404 masking (foreign/missing), 409/43000 illegal transitions, 401 shared body, 40000 unknown-field/bounds/malformed; student 403 on admin routes and real-login authorization are green in `ApprovalApiRealIntegrationTest` against Compose MySQL 8.

## 4. Verification tests (MySQL 8 and real API)

- [x] 4.1 Add unit tests for comment/reason normalization, unknown JSON 40000, page bounds, stable ordering, Long-string views, and the exact two-hour/start boundary. Evidence: `ApprovalRequestTest` 8/8 green (trim/null, astral code-point boundaries 250×😀=500 UTF-16 units pass, 501 cp fail; unknown fields), `ApprovalServiceTest` 12/12 green (winner/idempotent/409/404 mapping, inclusive 2h exemption incl. nanosecond edges), `ApprovalControllerMockMvcTest` 13/13 green. Command: `mvn -q test -Dtest=ApprovalRequestTest,ApprovalServiceTest,ApprovalControllerMockMvcTest -Dsurefire.failIfNoSpecifiedTests=false`.
- [x] 4.2 Add MySQL 8 integration tests proving pending slots are occupied, approve keeps slots, reject/cancel release slots, and rollback leaves booking/slots/approval/late side effects unchanged. Evidence: `ApprovalMysqlIntegrationTest` seeds V003/V004-compatible fixtures and proves slot occupancy/retention/release plus rollback restoration of booking, slots, approval records, violations, and credit. Compose MySQL 8 run: 8 tests, 0 failures/errors/skips.
- [x] 4.3 Add concurrency tests for duplicate approve/reject/cancel: one conditional winner, one approval record at most, no double slot deletion, and no duplicate late-cancel event/port; every repeated identical action returns HTTP 200 with the current BookingView. Evidence: `ApprovalMysqlIntegrationTest` runs synchronized starts for approve (8 threads), reject (4 threads), and late cancel (4 threads); every future must return the matching terminal `BookingView`; assertions prove one approval record, one late-cancel violation/-5 deduction, and final slot count 0. Strict Compose MySQL 8 rerun: 8/8 green.
- [x] 4.4 Add authorization/ownership tests for 401, student 403, foreign/missing/deleted cancellation 404 masking, and 409/43000 invalid transitions. Evidence: `ApprovalControllerMockMvcTest` 13/13 covers 401, 404 masking, 409/43000, and malformed/unknown 40000; `ApprovalApiRealIntegrationTest` uses the real security chain/login and proves student 403 against Compose MySQL 8.
- [x] 4.5 Add real API tests for all four endpoints and assert no external test silently skips; record exact failures instead of claiming pass. Evidence: `ApprovalApiRealIntegrationTest` starts a RANDOM_PORT server, obtains real login tokens, exercises all four endpoints, idempotent repeat, slot retention, ownership and authorization; explicit environment prerequisite is `BOOKING_MYSQL8_TEST=true` plus DB/Redis variables. Compose run: 2 tests, 0 failures/errors/skips.
- [x] 4.6 After T10 merges the required `ViolationPort`, rebase T09 onto T10 and run the real late-cancel violation/credit integration, asserting `LATE_CANCEL=-5`, `NO_SHOW=-10`, credit floor 0, and same-transaction state/slot/side-effect behavior. Evidence: apply base contains T10 commit `9a096c7`; `ApprovalMysqlIntegrationTest.earlyCancelHasNoViolationWhileLateCancelRecordsMinusFiveWithCreditFloor` and concurrent late-cancel test run through the real `ViolationPort`, proving early exemption, one `LATE_CANCEL=-5`, credit floor/single deduction, slot release, and rollback atomicity. Green against Compose MySQL 8.

## 5. Handoff, validation, and stop conditions

- [x] 5.1 Publish exact DTO/error examples to T08/T11 and request review of the approval package contract; do not modify their frontend paths. Evidence (authoritative handoff notes, unchanged frontend paths):
  - **T08 (student booking UI) — cancel**: `POST /api/v1/bookings/{id}/cancel`, authenticated owner only. Body optional; `{"cancelReason": string|null}`; trim, blank→null, max 200 Unicode code points (astral-aware); unknown JSON field ⇒ HTTP 400 `{code:40000,message:"invalid parameter",data:null}`. Success ⇒ HTTP 200 `{code:0,message:"success",data:BookingView(status=CANCELLED, cancelTime, cancelReason)}`; repeated identical cancel also 200 with current view (no side effects). Errors: foreign/missing/deleted ⇒ identical HTTP 404 `{code:40400,data:null}` (no enumeration); at/after start or opposite terminal state ⇒ HTTP 409 `{code:43000,data:null}` message "booking cannot be cancelled"; late cancel (<2h before start) succeeds but records a hidden LATE_CANCEL (-5) — UI must NOT fabricate violation info from this endpoint. BookingView ids are decimal strings, timestamps `yyyy-MM-dd HH:mm:ss` Asia/Shanghai.
  - **T11 (admin operations UI) — approvals**: `GET /api/v1/admin/approvals?pageNumber&pageSize` (defaults 1/10, 1..pageSize..100; out-of-bounds ⇒ 40000, no query). Records: only non-deleted `PENDING_APPROVAL`, ordered `createdAt ASC,id ASC` (server order must be preserved). `POST /api/v1/admin/bookings/{id}/approve` body `{"comment": string|null}` — trim, blank⇒null accepted, ≤500 cp. `POST /api/v1/admin/bookings/{id}/reject` body `{"comment": string}` — REQUIRED, trimmed 1..500 cp; blank/missing ⇒ 40000. Success both ⇒ 200 with `BookingView` (CONFIRMED/REJECTED). Repeated identical approve/reject ⇒ 200 current view (safe to retry); opposite/illegal transition (e.g., approve REJECTED/CANCELLED, reject CONFIRMED) ⇒ HTTP 409 `{code:43000,data:null}`. Non-ADMIN session on any admin route ⇒ HTTP 403 `{code:40300,message:"forbidden",data:null}` with session preserved; unauthenticated ⇒ HTTP 401 `{code:40100,...}`. Missing/deleted booking on admin actions ⇒ 40400.
- [x] 5.2 Run `cd booking-api && mvn verify`, the relevant MySQL/real-API test profile, `openspec validate add-booking-approval-cancellation --type change --strict`, and `git diff --check`; record actual output and environment prerequisites. Evidence: Compose provides MySQL 8.4.8 and Redis 7.4.8 with `BOOKING_MYSQL8_TEST=true` plus `DB_*`, `RESOURCE_MYSQL_*`, and `REDIS_*`; focused approval run was 41/41 green before the final concurrency additions, strict `ApprovalMysqlIntegrationTest` rerun is 8/8 green, and final full `mvn verify` is 266 tests, 0 failures/errors/skips, BUILD SUCCESS. `openspec validate add-booking-approval-cancellation --type change --strict --no-interactive` reports valid; `git diff --check` is clean.
- [x] 5.3 Audit `git status --short` and the diff for ownership: only `booking-api/.../approval/**`, its tests, and approved OpenSpec artifacts may change. Any SQL/common/resource/user/frontend/pom/config or unapproved `booking/**` change is a stop condition. Evidence: working tree adds are exactly `booking-api/src/main/java/com/yu030x/booking/approval/**` (10 files) and `booking-api/src/test/java/com/yu030x/booking/approval/**` (5 files) plus this change's tasks.md edits; pre-existing staged docs/** files were left untouched; no git add/commit executed.
- [x] 5.4 T09 apply may complete approval, normal cancellation, and port adaptation before T10 merges, but do not final-merge or mark the change complete until T07 is merged/rebased, T10's `ViolationPort` is merged, T09 has rebased onto T10, and the late-cancel integration gate passes. Evidence: apply base contains merged T07 commits `0d31cd5`/`cc3dd17` and T10 commit `9a096c7`; focused approval, real API, strict concurrent MySQL, and full Compose-backed verification gates pass. Change is ready for final review/merge without editing T07/T10-owned production paths.
