## 1. Preconditions and scope gate

- [x] 1.1 Verify the apply base is the reviewed main/T06 merge or rebase. T06 planning artifacts exist and have been reviewed in sibling worktree `D:/Projects/project1_campus/target/worktrees/add-resource-availability`, but are not merged; record the T06 merge/rebase commit, re-read the merged read-only/pure-function contract, and stop without implementation if that evidence is missing. Evidence: the worktree was fast-forwarded to apply base `025b400`, which contains T06 merge commit `2d5aee4`; the archived T06 design and merged `resource-availability` read-only/pure-function contract were re-read before implementation.
- [x] 1.2 Verify the reviewed T01 Redis foundation planning at `D:/Projects/project1_campus/target/worktrees/add-redis-concurrency-foundation/openspec/changes/add-redis-concurrency-foundation/` has merged/rebased. Record its merge/rebase commit and re-read the shared contract before apply; stop until then. Do not edit `pom.xml`, `application.yml`, SQL, common, auth, user, resource/availability, frontend, or deploy files. Evidence: apply base `025b400` contains Redis implementation tip `0b05aa6` and archive commit `f8d1671`; the archived Redis design plus merged shared Redis and backend dependency contracts were re-read before implementation.
- [x] 1.3 Confirm the implementation worktree is `D:/Projects/project1_campus/target/worktrees/add-concurrent-booking-core` on `codex/add-concurrent-booking-core`, with only the booking ownership paths permitted by this change. Confirmed after fast-forward: branch `codex/add-concurrent-booking-core` at `025b400`; existing staged `docs/**` files are preserved as unrelated user content and will be excluded from every T07 commit.

## 2. Booking contracts and persistence boundary

- [x] 2.1 Add the booking-owned request/response models for `CreateBookingRequest` and the exact `BookingView` field set, including unknown-field rejection, decimal-string resource ID, purpose trim/Unicode-code-point limit, attendee count, Shanghai time format, Long-as-string serialization, and nullable values.
- [x] 2.2 Add booking and `booking_slot` entities plus annotation-based mappers under the booking package against the frozen DDL/indexes; follow the repository's existing inline MyBatis SQL style, add no mapper XML resource, preserve `uk_resource_slot`, physical slot deletion semantics, and booking logical-delete filtering without changing SQL migrations. Evidence: `BookingMapper` (page/detail/active-count/blacklist reads) and `BookingOccupancyMapper` (batch slot insert, physical delete-by-booking) use inline MyBatis-Plus annotation SQL under `booking/mapper/**` with `BookingMapperConfiguration` (`lazyInitialization=true`, matching the resource/user pattern); no mapper XML added; entities unchanged against V003 DDL; renamed from `BookingSlotMapper` after a bean-name clash with the availability package broke full-context tests.
- [x] 2.3 Add the pure `[start,end)` 30-minute slot splitter and reject cross-day, non-`:00`/`:30`, nonzero-second/nanosecond, empty, or reversed intervals before it is called; keep the function independent enough for deterministic unit tests.

## 3. API and ordered business validation

- [x] 3.1 Expose `STUDENT POST /api/v1/bookings`, authenticated `GET /api/v1/bookings?pageNumber&pageSize&status`, and authenticated `GET /api/v1/bookings/{id}` with canonical Result/PageResult envelopes, HTTP 201 for create, HTTP 200 for list/detail, and no cancel/check-in/approval routes. Evidence: `BookingController` (STUDENT-only create via principal role check returning 201; list/detail 200; no lifecycle routes); `BookingControllerMockMvcTest` 6/6 green (201 envelope with string ids, 40000 unknown-field/malformed handling, page bounds/status filter, 40400 masking).
- [x] 3.2 Implement current-user list/detail queries with `user_id` ownership predicates, stable `createdAt DESC,id DESC` ordering, page bounds, optional status filtering, and 404/40400 masking for missing, deleted, or foreign IDs. Evidence: `BookingMapper.selectUserPage/selectActiveByIdAndUser` + `BookingService.list/detail`; unit-level masking/ordering verified in `BookingControllerMockMvcTest`; DB-level proof written in `BookingMysqlIntegrationTest.listAndDetailMaskForeignDeletedOrMissingBookings` but execution awaits external MySQL.
- [x] 3.3 Implement the 16-step core order from `docs/15-项目一开发实施手册.md:462-481`: request shape; `startTime < endTime`; same-day; `:00`/`:30` alignment with seconds and nanoseconds zero; `startTime > now`; duration; inclusive `maxAdvanceDays`; resource exists/not deleted/status `1`; capacity; global/resource closure; open-rule containment; user enabled/not deleted; blacklist (`start_date <= today <= end_date`); active-booking count (default `3`); resource/date lock; transaction. This aligns the older draft core sequence and does not expand behavior. Evidence: `BookingIntervalValidator` (steps 1-5), `BookingCreationGuard.checkAll` (steps 6-14 in order, inclusive advance/blacklist bounds, capacity only when non-null), `BookingLockCoordinator` (step 15), `BookingCreator.create` `@Transactional` (step 16); each step covered by `BookingIntervalValidatorTest` 6/6 and `BookingCreationGuardTest` 10/10 green.
- [x] 3.4 Consume T06 availability only as a read-only/pure input and repeat all conflict-sensitive resource, rule, closure, user, blacklist, active-count, and slot checks inside the lock; do not use cache or availability output as final booking authority. Evidence: `BookingCreator.create` re-invokes `BookingCreationGuard.checkAll` inside the lock-held transaction before any insert (verified by `BookingCreatorTest.rechecksCriticalRulesInsideTransaction`); availability output is never consulted on the creation path; slot occupancy relies solely on frozen `uk_resource_slot`.
- [x] 3.5 Define the optional step-17 handoff only: after a successful transaction commit, T12 may invalidate `resource:available-slots:{resourceId}:{date}`. If T12 is not merged, do not build a cache or invalidation path. If invalidation fails, rely on T12's database fallback for availability reads, keep the booking committed, and leave T07's Redis lock fail-closed policy unchanged. Evidence: no cache read/write/invalidation code exists anywhere in `com.yu030x.booking.booking` (path audit clean); the handoff remains defined by proposal/design/spec deltas as required while T12 is unmerged.

## 4. Locking, transaction, and lifecycle seam

- [x] 4.1 Implement the resource/date lock coordinator with key `booking:lock:{resourceId}:{bookingDate}`, `tryLock(3,TimeUnit.SECONDS)` wait time only, watchdog-compatible behavior, owner-checked `finally` unlock, independent resource/date domains, and fail-closed Redis/lock failure mapping to HTTP 409/code `43000`/`data:null` with `当前预约请求较多，请稍后重试` (`SYSTEM_BUSY`); never use a DB-only downgrade. Evidence: `BookingLockCoordinator` (wait-time-only tryLock, `isHeldByCurrentThread()` guarded unlock in `finally`, per resource/date keys, missing client/timeout/RedisException/interrupt all map to SYSTEM_BUSY, no DB fallback); `BookingLockCoordinatorTest` 7/7 green; real-Redis contention/parallelism evidence pending external Redis (5.3).
- [x] 4.2 Implement an independent transactional creator bean that re-checks the critical rules, chooses `PENDING_APPROVAL` or `CONFIRMED` from `needApproval`, generates a non-sequential business number, inserts one booking, splits all slots, and batch-inserts every slot in the same transaction. Evidence: `BookingCreator.create` is `@Transactional` on a bean separate from the lock wrapper, re-runs the guard, selects initial status from `resource.needApproval`, and uses `BookingNumberGenerator` (`BK+timestamp+10 random chars`, uniqueness/format verified by `BookingNumberGeneratorTest` 2/2); state selection and atomic insert order verified by `BookingCreatorTest` 5/5 green.
- [x] 4.3 Translate any `DuplicateKeyException` from slot insertion to HTTP 409/code `43000`/`data:null` with `该时段已被占用，请刷新后重试` (`SLOT_CONFLICT`); keep it distinct from `SYSTEM_BUSY`'s `当前预约请求较多，请稍后重试` even though the code is shared. Verify the booking row and all partial slots roll back and never swallow the exception as success; later frontend work must map by message/category without conflation. Evidence: `BookingCreator` catches `DuplicateKeyException` and rethrows `BizException(BOOKING_ERROR, SLOT_CONFLICT)` so the transaction rolls back (never swallowed); `BookingCreatorTest.slotDuplicateKeyRollsBackAndMapsToSlotConflict` and `...BookingInsertDuplicateIsAlsoTreatedAsConflict` assert code/message distinction; rollback-at-DB-level proof written in `BookingMysqlIntegrationTest.laterSlotConflictRollsBackBookingAndAllSlots`, execution awaits external MySQL.
- [x] 4.4 Define explicit booking domain action/release seams for T09/T10 (approval, reject, cancel, check-in, no-show and terminal slot release) without implementing those routes or exposing arbitrary `updateStatus(target)`. Evidence: `BookingLifecycleActions` interface names approve/reject/cancel/checkIn/markNoShow only (no target-state update), `PendingBookingLifecycleActions` placeholder throws until T09/T10 land, and `BookingSlotReleaseService.releaseTerminalSlots` provides the transactional physical slot release for terminal transitions; no lifecycle routes exist in `BookingController`.

## 5. Verification evidence

- [x] 5.1 Add pure-function tests for 14:00–14:30 (one slot), 14:00–16:00 (four slots), end excluded, adjacent half-open intervals, invalid alignment, cross-day, start-after-now, duration, capacity, closure, open-rule containment, blacklist date bounds, and active-booking limit. Evidence: `BookingSlotSplitterTest` 6/6 and `BookingIntervalValidatorTest` 6/6 green for slot/alignment/cross-day/past-start; `BookingCreationGuardTest` 10/10 green (fixed Clock) covering duration bounds, inclusive advance days, capacity-only-when-set, global/resource closure, open-rule containment (wrong start/day/no rules), user disabled/deleted, blacklist bounds via count stub, active-booking default limit of 3.
- [ ] 5.2 Add MySQL 8 integration tests proving booking-plus-all-slots atomicity, rollback after a later slot conflict, frozen unique-key behavior, initial approval/no-approval states, ownership masking, list ordering, and canonical error envelopes. Blocked on execution: `BookingMysqlIntegrationTest` (5 tests) is written against the V003 schema but this environment has no external MySQL (`Missing required MySQL environment variable: RESOURCE_MYSQL_URL or DB_URL`, recorded as explicit failure 5/5 errors); must be re-run against a private MySQL 8 endpoint.
- [ ] 5.3 Add real Redis integration tests for lock acquisition, three-second contention behavior, owner-only unlock, Redis failure mapping, and no global serialization across different resources/dates. External MySQL/Redis tests MUST fail or be marked unavailable with an explicit failure; they MUST NOT silently skip and be reported as passing. Blocked on execution: `BookingRedisLockIntegrationTest` (4 tests) is written but fails explicitly here (`REDIS_HOST must be set to a private Redis test endpoint`, recorded 1 error).
- [ ] 5.4 Run a 50–100 concurrent same-slot test and record that exactly one request returns 201, all other contenders return 409/43000, and exactly one booking plus complete slot set remains. Blocked on execution: `BookingConcurrencyIntegrationTest.sixtyConcurrentSameSlotRequestsProduceExactlyOneWinnerAndCompleteSlotSet` requires both external MySQL and Redis; both unavailable here (explicit context-load failure recorded).
- [ ] 5.5 Run independent-resource and independent-date concurrency tests and record evidence that requests are not globally serialized while correctness is preserved. Blocked on execution: `differentDatesOnSameResourceAreNotGloballySerialized` and `differentResourcesOnSameDateProceedInParallelWithPerResourceCorrectness` written with sub-2.5s progress assertions; awaiting external MySQL+Redis.
- [ ] 5.6 Reproduce the historical check-then-insert bug only in an isolated, non-deployed test fixture or evidence harness, capture the reproduction, and remove/disable the vulnerable path before final verification; do not retain or deploy the bug. Blocked on execution: the harness `historicalCheckThenInsertRaceIsContainedByTheUniqueKeyWithoutAnyLock` (8 lock-bypassing creator calls in the isolated test fixture) is written to capture the race being contained by `uk_resource_slot`; awaiting external MySQL+Redis. The production path always acquires the resource/date lock first.

## 6. Final planning/apply gate validation

- [ ] 6.1 Run `mvn verify` from `booking-api` after all required external dependencies and services are available, and record the real result (including any explicit unavailable-service failure). Real recorded result in this environment: `Tests run: 149, Failures: 1, Errors: 18, Skipped: 2 — BUILD FAILURE`. All 18 errors are explicit external-service unavailability (no DB_URL/RESOURCE_MYSQL_* and no REDIS_HOST), covering both new booking ITs and pre-existing User/Availability/RedisReal ITs; the 1 failure (`UserMapperRegistrationIntegrationTest`) reproduces identically on the clean base without T07 code and belongs to the user module. All non-external tests pass, including the 48 booking-package unit/MockMvc tests. Must be re-run to green once private MySQL/Redis endpoints are supplied.
- [x] 6.2 Run `openspec validate add-concurrent-booking-core --type change --strict --no-interactive` from the worktree and resolve every artifact/spec error. Evidence: validation passed with no issues/errors after implementation.
- [x] 6.3 Run `git diff --check`, `git status --short`, and a path audit proving only the two booking ownership trees plus this change's OpenSpec artifacts changed; stop and report any scope breach instead of broadening the change. Evidence: `git diff --check` clean; staged `docs/**` files pre-date this session and were left untouched; working-tree changes are exactly `booking-api/src/main/java/com/yu030x/booking/booking/**` and `booking-api/src/test/java/com/yu030x/booking/booking/**` plus this change's `openspec/changes/add-concurrent-booking-core/tasks.md`; no pom/application/sql/common/auth/user/resource/availability/frontend/deploy edits.
