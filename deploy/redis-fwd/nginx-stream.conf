pid /tmp/nginx.pid;
stream {
    server {
        listen 16379;
        proxy_connect_timeout 5s;
        proxy_timeout 300s;
        proxy_pass redis:6379;
    }
}
events { worker_connections 256; }
